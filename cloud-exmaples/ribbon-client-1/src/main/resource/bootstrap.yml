eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eurekaserver1/eureka/
    registry-fetch-interval-seconds: 3

  instance:
    lease-renewal-interval-in-seconds: 2
#    appname: ${spring.applicatioin.name}:${server.port}